<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content = "width = device-width, initial-scale = 1.0, minimum-scale = 1, maximum-scale = 1, user-scalable = no" />
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-title" content="HackerNewsGrid" />
        <link rel="apple-touch-icon" href="icon.png">
        <title>Hackernews Grid</title>

        <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">
        <link rel="stylesheet" href="app.css" type="text/css" media="screen" title="no title" charset="utf-8">
    </head>
    <body class="{{theme}}">
        <h1 class='nice-title'>HackerNews Grid</h1>
        <div class="main">
            {{#each links}}
                {{#if b2_image}}
                    <div class='card'>
                        <a class="img-link" href="{{url}}">
                            <img src="{{b2_image}}" alt=""/>
                            <div class="domain" style="position: absolute;bottom: 8px;left: 10px;background: rgba(0, 0, 0, 0.8);color: white;font-size: 0.8em;/*! width: 100%; */right: 10px;border-bottom-left-radius: 5px;border-bottom-right-radius: 5px;padding-top: 3px;padding-bottom: 3px;">{{host}}</div>
                        </a>
                        <div class='extra'>
                            <a class="link title" href="{{url}}"> {{title}} </a>
                            <a class="link comments" href="{{commentsUrl}}"> (comments)</a>
                        </div>
                    </div>
                {{/if}}
            {{/each}} 
            <div class='card'>
            </div>
        </div>
        <div class='foot-note'>
            Last updated at: {{lastUpdatedAt}}
        </div>
        <div class='foot-note'>
            <a class='theme-link' href="/light.html">Light</a>
            <a class='theme-link' href="/">Dark</a>
        </div>
        <div class='foot-note'>
        </div>
    </body>
</html>
